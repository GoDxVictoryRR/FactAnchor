# Backend API base URL (Next.js rewrites proxy /api → here in dev)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket base URL
NEXT_PUBLIC_WS_URL=ws://localhost:8000

# Used server-side only (not NEXT_PUBLIC_) for SSR data fetching
API_INTERNAL_URL=http://api:8000
